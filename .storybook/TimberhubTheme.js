import { create } from '@storybook/theming';

export default create({
  base: 'light',
  brandTitle: 'Timberhub storybook',
  brandImage: 'https://www.timberhub.com/logo.svg',
  brandTarget: '_self',
});
