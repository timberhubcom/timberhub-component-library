import React from 'react';
import { render } from '@testing-library/react';
import SelectInput from './SelectInput';

describe('SelectInput', () => {
  it('should', () => {
    render(<SelectInput name={'name'} />);
  });
});
